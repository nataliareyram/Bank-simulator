{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug (LLDB)",
            "program": "${workspaceFolder}/BankSimulator.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
